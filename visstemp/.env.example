# Frontend Next.js
# URL API Express (rewrite /api/* + SSR trang chi tiết)
BACKEND_URL=http://127.0.0.1:4000

# Tuỳ chọn — backend NestJS cũng đọc file này (.env.local) nếu bạn muốn gom cấu hình:
# DATABASE_URL=postgresql://USER:PASS@HOST:5432/DBNAME
